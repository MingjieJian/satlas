      module xnf_dop

      use code_dimensions, only: max_d, max_mion
      use var_types

      implicit none

      real(re_type) :: dopple(max_d, max_mion)
      real(re_type) :: xnfdop(max_d, max_mion)

      end module xnf_dop

!***********************************************************************
